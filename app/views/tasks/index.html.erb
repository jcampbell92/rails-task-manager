<h1>My tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task.id) %></li>
    <p><%= link_to "Delete Task", task_path(task), data: { "turbo-method": :delete } %></p>
  <% end %>
</ul>
<p><%= link_to "Create new task", new_task_path%></p>
